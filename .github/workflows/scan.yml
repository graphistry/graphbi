name: Scan
on:
workflow_dispatch:

  push:
    paths: [
      "/home/runner/work/graphbi/graphbi/graphbi/pbiviz/package-lock.json",
      "/home/runner/work/graphbi/graphbi/graphbi/pbiviz/package.json",
      "/home/runner/work/graphbi/graphbi/graphbi/pbiviz/pbiviz.json"
    ]
    tags:

  #daily at 8am
  schedule:
    - cron: '0 8 * * *'

  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: pbiviz
        working-directory: /home/runner/work/graphbi/graphbi/graphbi/pbiviz
        run: npm audit --production
